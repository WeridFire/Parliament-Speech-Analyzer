# ============================================
# Italian Parliament Speech Analyzer
# Python Dependencies
# ============================================

# Core Data Processing
pandas>=2.0.0
numpy>=1.24.0
python-dateutil>=2.8.0        # correct month arithmetic for fetch windows

# Configuration
PyYAML>=6.0                   # analyzers/config.yaml drives every feature flag

# Data collection
requests>=2.28.0
beautifulsoup4>=4.12.0
cloudscraper>=1.2.0           # optional transport for anti-bot protected hosts

# Optional: browser transport for hosts behind a JavaScript challenge
#   pip install playwright && playwright install chromium
# playwright>=1.40.0

# Machine Learning & NLP
scikit-learn>=1.3.0
sentence-transformers>=2.2.0

# Optional: Advanced NLP (for better keyword extraction)
spacy>=3.5.0

# Optional: Transformer-based Sentiment Analysis
# Enable via `sentiment.use_transformer` in backend/analyzers/config.yaml
transformers>=4.30.0
torch>=2.0.0
